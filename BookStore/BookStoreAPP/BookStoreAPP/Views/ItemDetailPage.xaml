<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BookStoreAPP.Views.ItemDetailPage"
             xmlns:viewmodels="clr-namespace:BookStoreAPP.ViewModels"
             x:DataType="viewmodels:ItemDetailViewModel"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <!--<Label Text="Title" FontSize="Medium" />
        <Entry Text="{Binding NewBook.Title, Mode=TwoWay}" FontSize="Medium" IsReadOnly="True" />
        <Label Text="Author" FontSize="Medium" />
        <Editor Text="{Binding NewBook.Author, Mode=TwoWay}" AutoSize="TextChanges" FontSize="Medium" Margin="0" IsReadOnly="True" />
        <Label Text="Publication Year" FontSize="Medium" />
        <Entry Text="{Binding NewBook.PublicationYear, Mode=TwoWay}" Keyboard="Numeric" FontSize="Medium" IsReadOnly="True" />
        <Label Text="ISBN" FontSize="Medium" />
        <Entry Text="{Binding NewBook.IsbnNumber, Mode=TwoWay}" FontSize="Medium" IsReadOnly="True" />
        <StackLayout Orientation="Horizontal" VerticalOptions="Center">
            <CheckBox IsChecked="{Binding NewBook.IsAvailable}" />
            <Label Text="Is Available" />
        </StackLayout>-->
    </StackLayout>
    
</ContentPage>